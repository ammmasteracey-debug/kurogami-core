# Kurogami Hub

This workspace is a scaffold for a cinematic Next.js 14 (App Router) + Tailwind + Framer Motion site called Kurogami Hub.

Run locally:

```bash
npm install
npm run dev
```

Pages:
- `/` cinematic landing page
- `/hub` dark command center dashboard
# kurogami-core